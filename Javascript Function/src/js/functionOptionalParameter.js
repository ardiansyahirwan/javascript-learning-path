/**
 * by default, function in javascript setting is optional parameter
 * you can set the parameter and its still run if parameter in function is null
 * parameter was null will rendered as undifined variable
 */

export function functionOptionalParameter ( text, text2, text3 )
{
   console.log( text, text2, text3 )
}